import { NumberIndexPipe } from './number-index.pipe';

describe('NumberIndexPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberIndexPipe();
    expect(pipe).toBeTruthy();
  });
});
